<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Node - Supply Chain Management</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="flex">

    <div class="flex-1 p-8 bg-gray-50">
        <div class="max-w-2xl mx-auto bg-white shadow-lg rounded-lg p-8">
            <h1 class="text-3xl font-bold mb-6 text-blue-600">
                <i class="fas fa-plus-circle mr-3"></i>Add Node
            </h1>
            
            <form action="/nodes" method="POST" class="space-y-6">
                <div>
                    <label class="block text-gray-700 font-medium mb-2">Node Name</label>
                    <input 
                        type="text" 
                        name="name" 
                        required 
                        class="w-full px-4 py-2 border border-blue-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="Enter Node Name"
                    >
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">Node Type</label>
                    <select 
                        name="type" 
                        required 
                        class="w-full px-4 py-2 border border-blue-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    >
                        <option value="">Select Node Type</option>
                        <option value="supplier">Supplier</option>
                        <option value="distributor">Distributor</option>
                        <option value="customer">Customer</option>
                    </select>
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">Location</label>
                    <input 
                        type="text" 
                        name="location" 
                        required 
                        class="w-full px-4 py-2 border border-blue-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="Enter Node Location"
                    >
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">Contact Information</label>
                    <input 
                        type="text" 
                        name="contact" 
                        required 
                        class="w-full px-4 py-2 border border-blue-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                        placeholder="Enter Contact Details"
                    >
                </div>

                <button 
                    type="submit" 
                    class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 transition duration-300 transform hover:scale-101"
                >
                    <i class="fas fa-save mr-3"></i>Add Node
                </button>
            </form>
        </div>
    </div>


    <div class="flex-1 p-8 bg-gray-50">
        <div class="bg-white shadow-lg rounded-lg p-8">
            <h1 class="text-3xl font-bold mb-6 text-blue-600">
                <i class="fas fa-eye mr-3"></i>View Nodes
            </h1>
            
            <div class="overflow-x-auto">
                <table class="w-full bg-white">
                    <thead class="bg-blue-100">
                        <tr>
                            <th class="p-3 text-left text-blue-600">ID</th>
                            <th class="p-3 text-left text-blue-600">Name</th>
                            <th class="p-3 text-left text-blue-600">Type</th>
                            <th class="p-3 text-left text-blue-600">Location</th>
                            <th class="p-3 text-left text-blue-600">Contact Info</th>
                            <th class="p-3 text-left text-blue-600">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% nodes.forEach(function(node) { %>
                            <tr class="border-b hover:bg-blue-50">
                                <td class="p-3"><%= node.n_id %></td>
                                <td class="p-3"><%= node.n_name %></td>
                                <td class="p-3"><%= node.n_type %></td>
                                <td class="p-3"><%= node.n_Location %></td>
                                <td class="p-3"><%= node.n_contact %></td>
                                <td class="p-3">
                                    <div class="flex space-x-2">
                                        <a href="/nodes/edit/<%= node.id %>" class="text-blue-500 hover:text-blue-700">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a href="/nodes/delete/<%= node.id %>" class="text-red-500 hover:text-red-700">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</body>
</html>